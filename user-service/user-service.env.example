SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8001

# JWT (used to validate access/refresh tokens)
JWT_SECRET=change-me-in-prod

# DB config (only required when SPRING_PROFILES_ACTIVE=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cloud_kitchen
DB_USERNAME=root
DB_PASSWORD=root
